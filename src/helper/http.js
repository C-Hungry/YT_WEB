import axios from "axios";
import { api as apiMap } from "../../global";
import { Message } from "element-ui";
import { Loading } from "element-ui";

//axios.defaults.baseURL = BASE_URL;

const httpHelper = {
  
};

export default httpHelper;
